<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="RatingBar" parent="android:style/Widget.Material.RatingBar.Small">
        <item name="colorControlNormal">@color/white</item>
        <item name="colorControlHighlight">@color/white</item>
        <item name="colorControlActivated">@color/rating_yellow</item>
        <!-- For lollipop and up -->
        <item name="android:progressBackgroundTint" tools:targetApi="lollipop">@color/white</item>
        <item name="android:progressTint" tools:targetApi="lollipop">@color/rating_yellow</item>
        <item name="android:secondaryProgressTint" tools:targetApi="lollipop">@color/rating_yellow
        </item>
        <item name="android:numStars">10</item>
        <item name="android:isIndicator">true</item>
    </style>
</resources>
